#include <stdio.h>
#include <conio.h>

void main()
{
  int multiply(int)
  int a result
  printf ("enter the value");
  scanf ("%d" &a);
    result=multiply(a)
  printf ("%d",result);
  getch();
}

int multiply (int a)
{
  int c;
  c=v1*v1;
  return c;
}