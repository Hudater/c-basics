#include <stdio.h>

void main()
{
  int multiply(int);
  int a, result;
  printf ("enter the value");
  scanf ("%d", &a);
  result=multiply(a);
  printf ("%d", result);
}

int multiply (int a)
{
  int c;
  c=v1*v1;
  return c;
}