#include <stdio.h>
#include <strings.h>

void main(){
    char char_in, *str_in, *buf;
    printf("Enter a single char: ");
    char_in=getchar();
    putchar(char_in);
    printf("\nEnter a string: ");
    *str_in=(buf,100,stdin);
    puts(str_in);
}